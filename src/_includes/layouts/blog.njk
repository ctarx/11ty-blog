{% extends "layouts/base.njk" %}

{% block main %}
<h3>My Notes and Articles</h3>
<ul>
  {% for post in collections.posts %}
    <li>
      <p>{{ post.date | postDate }} — <a href="{{ post.url }}">{{ post.data.title }}</a></p>
    </li>
  {% endfor %}
</ul>
{% endblock %}
